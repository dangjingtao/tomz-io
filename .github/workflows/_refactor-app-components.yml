name: One-time app component refactor

on:
  push:
    branches:
      - preview/weekly-v0-1
    paths:
      - .github/workflows/_refactor-app-components.yml

permissions:
  contents: write

jobs:
  refactor:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: preview/weekly-v0-1
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install pnpm
        run: npm install --global pnpm@10

      - name: Split App components and overlay CSS
        run: python3 scripts/refactor-app-components.py

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Unit tests
        run: pnpm test

      - name: Media pipeline tests
        run: pnpm run media:test

      - name: MiraDocs compatibility
        run: pnpm run verify:mira-docs

      - name: Root build
        run: pnpm run build

      - name: Root static output
        run: node scripts/verify-br003a-root-static.mjs

      - name: GitHub Pages build
        run: pnpm run build:github-pages

      - name: GitHub Pages static output
        run: EXPECTED_BASE=tomz-io pnpm run verify:static-output

      - name: Commit refactor and remove one-time helpers
        run: |
          git checkout -- src/content/content-times.generated.ts src/content/home-recent.generated.ts src/content/home-focus.generated.ts 2>/dev/null || true
          rm -rf .mira-cache dist
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm .github/workflows/_refactor-app-components.yml scripts/refactor-app-components.py
          git add src/App.tsx src/main.tsx src/styles.css src/components src/features/docs src/styles
          git diff --cached --check
          git commit -m "refactor: split app components and overlay styles"
          git push origin HEAD:preview/weekly-v0-1
